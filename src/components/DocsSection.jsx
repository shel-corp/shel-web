import StatusLine from './StatusLine.jsx';

export default function DocsSection() {
  return (
    <section id="docs">
      <h2>Docs</h2>
      <StatusLine className="page-status">REFERENCE MATERIAL — PENDING PUBLICATION</StatusLine>
      <p>Documentation is issued in controlled revisions. Public materials may lag the current operating standard.</p>
    </section>
  );
}
