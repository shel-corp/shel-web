export default function Environments() {
  return (
    <section>
      <h2>Environments</h2>
      <p className="status page-status">EXTERNAL INTERFACES — CONTROLLED ACCESS</p>
      <p>
        Provisioned environments are issued per policy. Public index is intentionally incomplete.
        If you have an authorization token, initiate routing.
      </p>
      <div className="cta"><a href="mailto:info@shelcorp.com">Initiate Routing</a></div>
    </section>
  );
}
